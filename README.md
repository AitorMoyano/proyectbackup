# 💾 NAS Backup System

Sistema completo de backups y RAID para Ubuntu Server.

## 🚀 Deploy Rápido

```bash
git clone https://github.com/tu-usuario/nas-backup-system.git
cd nas-backup-system
chmod +x deploy.sh
sudo ./deploy.sh